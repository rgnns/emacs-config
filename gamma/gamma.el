;;; gamma.el --- Gamma

;; Starting requires which need to be kept in order.
(require 'gamma-perf)
(require 'gamma-utils)
(require 'gamma-borg)
(require 'gamma-defaults)

(require 'gamma-ui)
(require 'gamma-ux)

(require 'gamma-vc)
(require 'gamma-dev)

(provide 'gamma)

;;; gamma.el ends here
