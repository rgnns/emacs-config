;;; gamma-pkgs.el --- Package administration.

(progn ; use-package
  (require 'use-package)
  (setq use-package-verbose t
	use-package-expand-minimally t))

(provide 'gamma-pkgs)

;;; gamma-pkgs.el ends here
